import BaseRepository from "../../BaseRepository.js";
import Open from "#models/Businness/open";

export default class OpenRepository extends BaseRepository {
    constructor() {
        super(Open);
    }
}